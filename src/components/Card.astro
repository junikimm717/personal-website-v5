---
interface Props {
	title: string;
	href: string;
}

const { href, title } = Astro.props;
---

<div class="link-card">
	<a href={href}>
		<h2>
			{title}
		</h2>
		<slot />
	</a>
</div>

<style scoped>
  h2 {
    @apply text-blue-800 dark:text-blue-200 text-lg font-semibold
  }
</style>
